# Task Manager — React + TypeScript + FastAPI

A full-stack task manager with create, read, update, and delete operations.



## Run the frontend
In another terminal:
```bash
cd frontend
npm install
npm run dev
```
Open the Vite URL shown in the terminal (normally http://localhost:5173).

